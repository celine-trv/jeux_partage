{% extends "base.html.twig" %}

{% block title %}
        BackOffice | Modification catégorie
{% endblock %}


{% block body %}
        <h1 class="h1 mt-3 mb-4">Modification de la catégorie <span>{{category.name}}</span></h1>

        {{ form_start(formCategory, {'attr' : {"class": "col-md-12 mx-auto py-4 row justify-content-around"} })}}

                {{ form_row(formCategory.name, { 'label' : "Catégorie"}) }}
		{{ form_row(formCategory.save, {'label' : "Modifier", 'attr' : {'class': 'btn-jp mx-auto mt-4'}}) }}

        {{ form_end(formCategory)}}




{% endblock %}